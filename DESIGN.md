# LightTranslate 视觉与交互基准

## 核心气质

界面必须精致、高级、安静，同时保持轻量和低认知负担。高级感来自比例、留白、字体层级、材质克制和细节一致性，不依靠堆叠组件。

## 视觉方向

- 深墨灰底色，避免纯黑。
- 香槟金只用于主操作、状态和少量强调。
- 象牙白正文，次级文字逐级降低透明度。
- 大圆角只用于窗口外壳，内部卡片圆角更小。
- 阴影柔和且范围大，不做廉价发光描边。
- 可使用极轻的顶部高光与材质层次，但不使用厚重玻璃拟态。
- 动效短、轻、少，不弹跳，不做大幅位移。
- 正式图标采用两片折页交汇的抽象形态，石墨黑底配象牙白与香槟金，托盘连续状态叠加鼠尾草绿圆点。

## 交互原则

1. 日常路径最多一次快捷键和一次框选。
2. 主窗口不承担后台管理功能。
3. 译文为第一视觉层，源文可见但不抢占空间。
4. 看懂、精校、术语属于按需功能，不干扰普通翻译。
5. 错误在浮窗内部处理，不弹系统级错误框。
6. 任何自动监听默认关闭，用户主动开启后才生效。
7. 设置项保持少量、可理解、可恢复。

## 禁止方向

- 管理后台式侧栏和复杂导航。
- 大面积高饱和色。
- 多引擎并排结果。
- 密集图标工具栏。
- 过多描边、发光和玻璃层。
- 为展示技术能力而暴露模型参数、Token 或内部日志。

## 当前阶段

当前版本采用 WPF/.NET 10 单文件架构、托盘与全局快捷键、DPAPI 密钥存储、PP-OCRv5 中英日本地 OCR；DeepSeek V4 Flash 普通翻译使用 HIGH，看懂与精校使用 MAX。
